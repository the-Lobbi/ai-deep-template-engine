apiVersion: v1
kind: Service
metadata:
  name: deep-agent-harness
  labels:
    app: deep-agent-harness
    component: orchestrator
spec:
  type: ClusterIP
  ports:
    - port: 8000
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: deep-agent-harness
  sessionAffinity: None
